# Security

MergeWeather is local-first and read-only.

- Git refs are validated before use.
- Git commands are executed without a shell.
- History collection is bounded.
- MergeWeather does not upload repository data.
- MergeWeather does not intentionally execute repository hooks.

If you find a security issue, use GitHub's private vulnerability reporting rather than posting sensitive details publicly.
